SqueakView Google Drive Upload Setup
====================================

Purpose
-------
This guide explains how to configure Google Drive uploads for SqueakView using `rclone`.

Current SqueakView behavior
---------------------------
At the end of a run, SqueakView can prompt:

  Upload to Google Drive now?

If the user clicks `Yes`, SqueakView runs:

  rclone copy <run_dir> gdrive:SqueakViewUploads/<run_name> \
    --exclude "*.mp4" \
    --exclude "*.svo" \
    --exclude "*.svo2" \
    --progress

Important:
- uploads are a copy, not a move
- local run data remains on the Jetson
- video files are excluded by default


Prerequisites
-------------
Make sure `rclone` is installed:

  rclone version

If that command fails, install `rclone` first.


Step 1: Configure a Google Drive remote
---------------------------------------
Run:

  rclone config

Create a new remote with this exact name:

  gdrive

Recommended answers:
- `n` for new remote
- name: `gdrive`
- storage type: `drive`
- client_id: press Enter unless you intentionally use your own OAuth app
- client_secret: press Enter unless you intentionally use your own OAuth app
- scope: choose full access if you want normal upload behavior
- root_folder_id: press Enter
- service_account_file: press Enter
- advanced config: `n`
- auto config: `y` if browser auth works on this machine

After authentication, confirm the remote and quit config.


Step 2: Verify the remote
-------------------------
List the top level of the drive:

  rclone lsd gdrive:

If this works, authentication is valid.


Step 3: Create the SqueakView upload folder
-------------------------------------------
Create the folder SqueakView expects:

  rclone mkdir "gdrive:SqueakViewUploads"

You only need to do this once.


Step 4: Manual smoke test
-------------------------
Test with an existing run directory:

  rclone copy "/home/jetson/Desktop/SqueakView/runs/<run_dir_name>" \
    "gdrive:SqueakViewUploads/<run_dir_name>" \
    --exclude "*.mp4" \
    --exclude "*.svo" \
    --exclude "*.svo2" \
    --progress

Then verify:

  rclone ls "gdrive:SqueakViewUploads/<run_dir_name>"


How SqueakView uses the remote
------------------------------
The current GUI assumes:

- remote name: `gdrive`
- destination folder: `SqueakViewUploads`

So if uploads are failing, first verify these:

  rclone lsd gdrive:
  rclone lsd "gdrive:SqueakViewUploads"


Changing Google accounts
------------------------
If you want to use a different Google account, reconfigure the remote:

  rclone config

Then either:
- edit remote `gdrive`, or
- delete and recreate remote `gdrive`

SqueakView currently expects the remote to still be named `gdrive`.


Common failure modes
--------------------
1. `rclone: command not found`
- install `rclone`

2. `didn't find section in config file`
- the `gdrive` remote is not configured yet

3. `directory not found`
- create `gdrive:SqueakViewUploads` with `rclone mkdir`

4. auth/browser issues
- rerun `rclone config`
- reauthenticate the `gdrive` remote

5. SqueakView upload prompt appears but upload fails
- test the same `rclone copy` command manually in terminal
- confirm the remote name is still `gdrive`


Useful commands
---------------
Check configured remotes:

  rclone listremotes

Inspect Google Drive root:

  rclone lsd gdrive:

Inspect upload folder:

  rclone lsd "gdrive:SqueakViewUploads"

Manually upload a run:

  rclone copy "/home/jetson/Desktop/SqueakView/runs/<run_dir_name>" \
    "gdrive:SqueakViewUploads/<run_dir_name>" \
    --exclude "*.mp4" \
    --exclude "*.svo" \
    --exclude "*.svo2" \
    --progress


Notes
-----
- This setup is independent of FLIR vs ZED capture.
- Excluding `.mp4`, `.svo`, and `.svo2` is hardcoded in the current GUI upload path.
- If the destination folder or remote name changes in code later, update this document too.
